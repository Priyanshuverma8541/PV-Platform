# API Gateway

Proxy gateway for the PV Platform.

## Run locally

1. Copy `.env.example` to `.env`
2. Install dependencies with `pnpm install`
3. Start the gateway with `pnpm dev`

## Proxies

- `/api/auth/*` -> auth service
